# Optional: add your key so the model writes in-character dialogue from bounded knowledge.
# Secrets are read by the local Vite API only, never VITE_ client variables.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
